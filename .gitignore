node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Ignore .env file
.env
Make sure to save the changes to your .gitignore file after adding the .env entry. This will ensure that the .env file is ignored and not tracked by Git.

By adding .env to your .gitignore, the file will no longer be included in your Git repository. Remember not to commit or push the `.




